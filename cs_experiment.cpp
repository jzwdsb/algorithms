//
// Created by manout on 17-9-26.
//


#include <cassert>
#include "common_use.h"


int cs_experiment(const int N, vector<int> a, vector<int> b, vector<int> c)
{
	int ret = 0, count = 0;
	int a_max = 0, b_max = 0, c_max = 0;
	assert(N == a.size() && a.size()== b.size() && b.size() == c.size());
	vector<bool> has_people(N, false);
	while(count not_eq N)
	{
		
		++count;
	}
}
