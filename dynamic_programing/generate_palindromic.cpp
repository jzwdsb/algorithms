//
// Created by manout on 18-4-9.
//

#include <bits/stdc++.h>


using namespace std;

using LL = long long ;

LL generate_palindromic(string& str)
{
    vector<vector<LL>> mat(str.length() + 1, vector<LL>(str.length() + 1, 0));
    for (int i = 1; i < str.length(); ++i)
    {
        for (int j = 1; j + i - 1 < str.length(); ++j)
        {
        
        }
    }
}